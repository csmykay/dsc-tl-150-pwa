# TL-150 credentials — copy to config/.env (never commit .env)
DSC_HOST=192.168.1.10
DSC_USER=admin
DSC_PASS=
DSC_PIN=

# First boot only (before settings.txt exists): optional seed for migrate — do NOT set if you use settings.txt
# DSC_ZONE_LIST=1-6
# DSC_ZONE_COLUMNS=1

# Panel options: edit settings.txt on the host (see settings.txt.example)
