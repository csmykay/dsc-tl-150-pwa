# TL-150 credentials only — copy to config/.env
DSC_HOST=192.168.1.10
DSC_USER=admin
DSC_PASS=
DSC_PIN=

# Titles, zones, poll interval, arm mode, etc.: see config/settings.txt (settings.txt.example)
